This 'dev' module serves the purpose of exposing endpoints that facilitate quick development speed.

It allows frontend devs to READ(only) data of any entity on the backend.

All admin endpoints that READ data are exposed to this module, plus additional endpoints that allow them read any users tokens(all token types)